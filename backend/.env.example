PORT=3000
JWT_SECRET=change_me_use_a_long_random_string
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_me

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Import GitHub + LLM via OpenRouter (optionnel — laisser vide pour désactiver)
OPENROUTER_API_KEY=
AI_MODEL=mistralai/mistral-small-3.1-24b-instruct:free

# Persistance — laisser vide pour utiliser process.cwd() (dev)
# En production : chemins absolus vers un volume persistant
DB_PATH=
UPLOADS_DIR=
