PORT=3000
JWT_SECRET=change_me_use_a_long_random_string
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_me

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Import GitHub + LLM — un seul provider suffit (Mistral prioritaire si les deux sont définis)
MISTRAL_API_KEY=
OPENROUTER_API_KEY=
# Laisser vide pour le modèle par défaut du provider choisi
AI_MODEL=

# Persistance — laisser vide pour utiliser process.cwd() (dev)
# En production : chemins absolus vers un volume persistant
DB_PATH=
UPLOADS_DIR=
